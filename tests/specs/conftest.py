"""Fixtures compartidas para los tests de specs SDD (.ai/specs/**).

Estos stubs se completan a medida que se implementa cada feature. No importan
módulos de la aplicación todavía porque el código base aún no existe (ver
CLAUDE.md / .ai/skills/pytest-testing/SKILL.md, regla 2).
"""
